document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-mail-form]');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=`${data.get('type')} enquiry from ${data.get('name')}`;const body=`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nPhone: ${data.get('phone')||'Not provided'}\nEnquiry: ${data.get('type')}\n\n${data.get('message')}`;window.location.href=`mailto:support@hork.in?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`})});
